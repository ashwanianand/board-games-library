{
  "name": "board-games-library",
  "version": "1.0.0",
  "description": "Personal board game collection website",
  "scripts": {
    "build": "node src/build.js",
    "dev": "node src/build.js --watch",
    "clean": "rm -rf docs"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "glob": "^11.0.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
